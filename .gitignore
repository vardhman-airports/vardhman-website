# Ignore node_modules (if not already present)
node_modules/
*node_modules/
public/videos/


# Ignore specific folders in public/ (if you want some parts but not others)
# Example:
# public/data/
# public/images/

# Ignore other folders/files if needed
*.log
.env
.DS_Store
Thumbs.db

# Data files that should persist on server
out/careers.json
out/pov.json
out/sessions/*.json
out/sessions/

# But keep the folder structure in git
!out/
!out/.gitkeep
!out/sessions/
!out/sessions/.gitkeep